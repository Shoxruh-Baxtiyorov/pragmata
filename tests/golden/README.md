# Golden set (неделя 4)

Сюда кладутся размеченные клипы + ожидаемые события (`<clip>.expected.json`).
CI гоняет пайплайн в `--offline --sink jsonl` режиме и сверяет фактические события с ожидаемыми.
